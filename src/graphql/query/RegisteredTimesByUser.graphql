query RegisteredTimesUserByUser($id: ID!) {
  registeredTimes(where: { user: { id: $id } }) {
    id
    created_at
    user {
      id
      name
      username
      email
      role {
        id
        name
      }
    }
  }
}
